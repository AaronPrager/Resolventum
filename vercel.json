{
  "buildCommand": "npm ci && cd server && npm ci && npx prisma generate && (npm run db:migrate:deploy || echo 'Migrations skipped or failed - continuing build') && cd ../client && npm ci && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

