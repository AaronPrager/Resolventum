{
  "buildCommand": "npm ci && cd server && npm ci && npx prisma generate && npx prisma migrate deploy && cd ../client && npm ci && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}

